import { AlwaysRefreshRouteReuseStrategy } from './always-refresh-route-reuse-strategy';

describe('AlwaysRefreshRouteReuseStrategy', () => {
  it('should create an instance', () => {
    expect(new AlwaysRefreshRouteReuseStrategy()).toBeTruthy();
  });
});
